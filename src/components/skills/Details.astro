---
import CraftLayout from "../../layouts/CraftLayout.astro";
import type { SkillId } from "../../models";

type Props = {
  skill: SkillId;
};

const { skill } = Astro.props;
---

<CraftLayout title={skill}>{skill}</CraftLayout>
