const WORDS_PER_MIN = 200; //275; // wpm

const IMAGE_READ_TIME = 12; // in seconds

const CHINESE_KOREAN_READ_TIME = 500; // cpm

const IMAGE_TAGS = ["img", "Image"];

export { WORDS_PER_MIN, IMAGE_READ_TIME, CHINESE_KOREAN_READ_TIME, IMAGE_TAGS };
